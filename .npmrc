# Expo / React Native need a flatter layout than pnpm's default isolation.
node-linker=hoisted
# Keep path-scurry on lru-cache v10 (v5 breaks PathScurry → Class extends undefined).
public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=*prettier*
